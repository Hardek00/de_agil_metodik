FROM python:3.11-slim

WORKDIR /app

# Installera Flask och curl för health check
RUN apt-get update && apt-get install -y curl && rm -rf /var/lib/apt/lists/*
RUN pip install flask

# Kopiera applikationsfil
COPY app_2.py .

# Exponera port 5001
EXPOSE 5001

# Starta databas-appen
CMD ["python", "app_2.py"]
